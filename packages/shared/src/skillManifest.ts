/**
 * Skill Manifest Standard (S05)
 * Re-exported from @mindpal/protocol - the single source of truth.
 */
export {
  validateManifest,
} from '@mindpal/protocol';

export type {
  SkillLayer,
  SkillToolDeclaration,
  BuiltinSkillManifest,
  ExternalSkillManifest,
  ManifestValidationResult,
} from '@mindpal/protocol';
