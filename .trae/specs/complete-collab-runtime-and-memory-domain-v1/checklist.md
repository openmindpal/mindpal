- [x] 非单主提交/租约冲突返回 SINGLE_WRITER_VIOLATION，且写入 collab.single_writer.violation
- [x] toolPolicy 拒绝时返回冲突语义并写入 collab.policy.denied（字段完整）
- [x] envelopes 写入强制 correlationId，且 envelopes/events 持久化 policySnapshotRef + correlationId
- [x] events/envelopes/diagnostics 可按 correlationId 串联回放（最小链路可在 e2e 断言）
- [x] memory：space/user 隔离严格生效（跨 space/subject 不可读取）
- [x] memory search/snippet 默认脱敏（不泄露 token/key 明文）
- [x] memory：session_context 支持读写/覆盖更新/清除并可恢复读取
- [x] memory：writePolicy 支持 confirmed/approved/policyAllowed
- [x] memory：导出+清除接口可用且写入审计摘要
- [x] e2e collab runtime 与 e2e memory 通过
