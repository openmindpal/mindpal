- [x] oauth_states/oauth_grants 表与 repo 落地（state 单次使用 + TTL）
- [x] `POST /oauth/authorize` 返回 authorizeUrl 且审计摘要正确
- [x] `GET /oauth/callback/:provider` 校验 state 并托管 token 到 SecretRecord（不暴露明文）
- [x] `POST /oauth/:provider/refresh` 可刷新并更新 SecretRecord（摘要审计）
- [x] token endpoint 出站受 allowedDomains 约束（默认拒绝）
- [x] e2e 覆盖成功链路与 state 失效/越权拒绝
- [x] README 补齐 OAuth 回调托管用法与安全约束

- [x] Provider 配置表与配置 API 落地（按 connectorInstanceId+provider），且审计不含密钥明文
- [x] `GET /oauth/providers` 返回支持的 provider 列表（wecom/dingtalk/feishu/google）与能力元数据
- [x] `POST /oauth/authorize` 对非 mock provider 可按配置生成 authorizeUrl（支持 PKCE 与 extra params）
- [x] `GET /oauth/callback/:provider` 可按配置完成 code→token 托管（受 egressPolicy.allowedDomains 约束）
- [x] `POST /oauth/:provider/refresh` 可按配置刷新并更新 SecretRecord（审计不含敏感明文）
- [x] e2e 覆盖 wecom/dingtalk/feishu/google 的最小闭环与拒绝链路（越权/过期/allowedDomains）
