- [x] 高风险动作在缺少 `runId/stepId/policySnapshotRef` 任一字段时被拒绝，且错误语义稳定
- [x] `errorCategory` 在 API 返回、audit_events 落库、SIEM 导出中枚举一致
- [x] `audit_events` 保持 append-only，update/delete 操作失败
- [x] 审计 outbox 写入失败会导致业务事务回滚，不出现“业务成功但审计缺失”
- [x] SIEM test/backfill/dlq 全链路可观测、可回放、可重试并具备阈值告警
- [x] `/models/chat` 支持 `outputSchema` 强校验，输出不合规时返回稳定错误码
- [x] 模型候选路由、熔断与降级摘要可观测，且审计摘要不含敏感信息
- [x] 模型路由策略发布受评测准入门槛约束，未通过评测不可发布
- [x] 模型限流支持 tenant/user/scene 维度命中统计并返回明确错误
- [x] 模型预算硬上限命中返回明确错误并保留审计证据
- [x] `hashchain.test.ts` 与 e2e 审计段、e2e model gateway 相关用例通过
