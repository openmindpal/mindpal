name: ci

on:
  push:
  pull_request:
  workflow_dispatch:
    inputs:
      run_web_e2e:
        description: "Run web e2e job (WEB_E2E=1)"
        required: false
        default: "false"
  schedule:
    - cron: "0 3 * * 0"

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: openslin
          POSTGRES_USER: openslin
          POSTGRES_PASSWORD: openslin
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U openslin -d openslin"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
      redis:
        image: redis:7
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    env:
      POSTGRES_HOST: 127.0.0.1
      POSTGRES_PORT: "5432"
      POSTGRES_DB: openslin
      POSTGRES_USER: openslin
      POSTGRES_PASSWORD: openslin
      REDIS_HOST: 127.0.0.1
      REDIS_PORT: "6379"
      # Disable eval admission gate in tests to allow specific error codes
      EVAL_ADMISSION_REQUIRED_KINDS: ""
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"
      - run: pnpm install --frozen-lockfile
      - run: node scripts/secret-scan.mjs
      - name: Lint (root-level)
        run: pnpm run lint
      - name: Architecture boundary scan
        run: pnpm run boundary-scan
      - run: pnpm run build
      - name: Run all tests
        run: pnpm test
      - name: Run eval regression gate
        run: pnpm --filter apps/api run eval:ci

  web-e2e:
    if: >-
      github.event_name == 'schedule' ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.run_web_e2e == 'true')
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: openslin
          POSTGRES_USER: openslin
          POSTGRES_PASSWORD: openslin
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U openslin -d openslin"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
      redis:
        image: redis:7
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    env:
      POSTGRES_HOST: 127.0.0.1
      POSTGRES_PORT: "5432"
      POSTGRES_DB: openslin
      POSTGRES_USER: openslin
      POSTGRES_PASSWORD: openslin
      REDIS_HOST: 127.0.0.1
      REDIS_PORT: "6379"
      API_PORT: "3001"
      API_CORS_ORIGINS: "http://127.0.0.1:3000"
      NEXT_PUBLIC_API_BASE: "http://127.0.0.1:3001"
      WEB_E2E: "1"
      API_BASE: "http://127.0.0.1:3001"
      WEB_BASE: "http://127.0.0.1:3000"
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"
      - run: pnpm install --frozen-lockfile
      - run: node scripts/secret-scan.mjs
      - run: pnpm run build
      - run: pnpm --filter apps/api run db:seed
      - name: start api
        run: |
          pnpm --filter apps/api start &
          for i in {1..60}; do
            curl -sf http://127.0.0.1:3001/healthz >/dev/null && break
            sleep 1
          done
      - name: start web
        run: |
          pnpm --filter apps/web start -p 3000 &
          for i in {1..60}; do
            curl -sf http://127.0.0.1:3000/ >/dev/null && break
            sleep 1
          done
      - run: pnpm --filter apps/web test
